# Session · 2026-05-28 · silero VAD INV-17 + 4 件套 patch + LLM 联网决策 X1 + 网易云能力 audit

> 范围:从上一对话 compacted 出。本日主线 = silero VAD 集成 INV-17 上线 + 4 件套
> 杂项 fix + LLM enable_search 死开关接入 + 网易云能力盘点(发现已实现 18
> actions PM 不知道)。proactive cron 顺手 audit 发现 DB 几乎全 dead。

---

## 1 · TL;DR

- **INV-17** silero VAD 集成完成 · `@ricky0123/vad-web@0.0.30` + `ort 1.26.0` ·
  误触发实测 → 阈值从默认 0.3 升 0.6(静息底噪误触发)
- **4 件套 patch** ship:touch NameError 恢复 / opencc 繁转简 / 日历 EKAlarm
  绑定 / VAD 默认值调整
- **LLM 联网决策 X1** ship:`config.yaml enable_search` 死开关 + 接入位置 C
  (LLM client `_dispatcher` 注入 search hint)
- **网易云能力 audit**:18 actions 已实现 PM 不知道(netease_web 7 + netease_local 7
  + media 5 = 19 总计 · 缺 1 是 PM 报 "18" 时漏数 like_current 的 now_playing 前置)
- **proactive cron audit**:DB 3 天 10 行 · 几乎全 dead(rolling summary 同期)
- **lessons #24-#29 沉淀** · 主轴 = audit 别跟用户 narrative 单点 / patch 间真机
  baseline / verify 用户用对模式 / surface symptom 超物理上限 / 推 maintainer
  baseline 前 verify

---

## 2 · 本轮主线

### 2.1 silero VAD 集成 INV-17

- 集成方:`@ricky0123/vad-web@0.0.30` 浏览器端 ort runtime 1.26.0
- 集成前:原 vad-web 0.0.x 老版本 model 不准 / 浏览器 worker 兼容性问题
- 关键 trial:阈值 0.3(默认)在 PM 真机静息状态被底噪误触发 → 调 0.6
- ship 后真机:VAD 触发率回归正常 · 不再静息抖动 fire

### 2.2 4 件套 patch

- **touch NameError 恢复**:`useAudio.ts` 路径 touch 旧 hook 调用残留 NameError
  · sweep import 修
- **opencc 繁转简**:LLM 输出偶发繁体字 · backend layer 加 opencc s2t 简化
  转换路径
- **日历 EKAlarm 绑定**:`apple_calendar.py` create_event 加 alarm_minutes_before
  支持 · EKAlarm 真绑到 event(之前 silently 忽略此 param)
- **VAD 默认值调整**:0.3 → 0.6(配合 2.1 trial 结论)

### 2.3 LLM 联网决策 X1 · enable_search 死开关接入位置 C

- `config.yaml` 加 `enable_search: bool` 死开关 · 默 off
- 接入位置 C = `backend/llm/client.py` `_dispatcher` 内 · request kwargs 注入
  `enable_search` hint(provider 接 LLM 联网功能时识别)
- 决策依据:PM 不想给 LLM 默认联网权(隐私 / 不可控搜索路径)· 但要预留开关供
  特定场景启用 · 接入位置 C 让所有 provider 路径一处生效不漏

### 2.4 网易云能力 audit

实证发现:backend 已实现 19 actions PM 不知道(此前以为只 7)。三套 cap 分布:
- **media** (5) · next_track / previous_track / play_pause / now_playing / set_volume
- **netease_web** (7) · daily_recommend / personal_fm / play_song / play_playlist /
  play_playlist_by_id / like_current / search
- **netease_local** (7) · play_song / play_playlist / pause / resume / stop /
  next_in_queue / now_playing

跨 cap 重叠 action 标记(now_playing × 2 · play_pause/pause/resume × 2 ·
next_track/next_in_queue × 2)→ tool 路径混乱 latent bug(2026-05-31 凌晨 PM 真机
撞上 · 见 SESSIONS 2026-05-30-to-31 Pit 2)。

### 2.5 proactive cron audit

DB `proactive_actions` 表 3 天 10 行 · 几乎全 dead。原因待 INV-13 follow-up
audit(本日只盘点 · 不修)。

---

## 3 · Lessons 沉淀(#24-#29 候选)

- **#24** audit 别跟用户 narrative 单点 · enumerate 同型判定
- **#25** patch 间必须真机 baseline · 不能盲叠
- **#26** patch 前 verify 用户用对模式 + 代码真 ship 到运行环境
- **#27** surface symptom 超出方案物理上限 · challenge 方案本身
- **#28** 推 "对齐 maintainer baseline" 前必须 verify 在我们环境真跑得起
- **#29** audit CC 实施时必查 实施≠调研推荐 · smoke ≠ 真测

完整文案见 `docs/LESSONS.md` 表第 #24-#29 行。

---

## 4 · Backlog 挂着

- **rolling summary 89 行全空**(PM 同日发现 · 见 SESSIONS 2026-05-29 §3)
- **proactive cron DB dead** 调研 · 等单独 follow-up audit
- **网易云 18/19 actions 真机覆盖率**:仅 daily_recommend / play_song(keyword)
  /search 历史走过 · 其它待 PM 真机扫(2026-05-31 凌晨 19 actions 测试矩阵 P0-P2
  推给 PM 醒后跑)

---

## 5 · 衔接

- 次日(2026-05-29)主线 = ja TTS 不发声根因 + proactive crash + 网易云 4 件套
  Patch ABCD ship
- 见 `docs/SESSIONS/2026-05-29.md`
