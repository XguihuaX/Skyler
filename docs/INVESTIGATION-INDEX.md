# INVESTIGATION 索引

> 跨 INVESTIGATION.md + INVESTIGATION-2.md 的逐刀索引。
> 每行：日期 | 主题 | 一句话结论 | 位置（文件 + 节标题）

INVESTIGATION.md 已封存（2086 行）；INVESTIGATION-2.md 已封存；INVESTIGATION-3.md 1098 行封存。
INVESTIGATION-4.md **1,120 行封存**（子轨 B §1-§3 评估 + §3.2.1 / §3.7 P2 实施收口完整覆盖）。
INVESTIGATION-5.md 自 2026-05-20 prompt caching 子轨 A 勘查与实施起用（已 ship 收口）。
INVESTIGATION-6.md **915 行封存**(子轨 B 实施前 3 刀 P3 / P1.media / P1.apple_calendar 完整收口;dispatcher 模板 6 要点 + lesson #7/#8 沉淀)。
INVESTIGATION-7.md 自 2026-05-21 子轨 B 实施收尾(P1.bilibili + P1.netease)起用,P1.netease ship 后**子轨 B 整轮 closed**(§ 收尾归档 B.1-B.5)。
**INVESTIGATION-8.md 1,560 行封存**(Phase 1 §1 audit 完整覆盖 — §1.1 句间停顿 + §1.2 TTS 链路 + §1.3 Fish API stage 1+2 + §1.4 voice ↔ character ↔ language + §1.5 sanitize bug audit + §1 收口 + 7 Q-A PM 拍板;5 决策最终三档 + 4 lesson + Option A1 lock + Hard Req per-provider 双重隔离 + cid=1→cid=101 数据迁移方案 lock);**Phase 2 实施迁 INVESTIGATION-9.md**(等 PM "听完 WAV β lock" 通知后起用)。
INVESTIGATION-10.md 自 2026-05-22 v4.0 立绘馆 voice greeting feature 起用(Phase 2 closed 后独立主线;PM 提前上传音频纯 storage+serve,立绘馆 onEnter 随机播放)。
INVESTIGATION-9.md 自 **2026-05-22 Phase 2 启动**起用(TTSProvider 抽象层 + Fish s2-pro 集成 + sanitize A1 fix + cid=1→cid=101 数据迁移收尾刀);§1 sanitize A1 fix ship 完成(159 case PASS / 0 regression)。
**INV-11** 系列(`docs/INV-11-*.md`)2026-05-25/26 主线 · GSV (GPT-SoVITS) 真接入 + 全角色 provider × model × voice paradigm:Stage 0 LLM output audit / Stage -1 prompt experiment(V3 success)/ Stage 1 GSV mai_v4 真接入(emotion-aware ja TTS)/ Stage 1.5 全角色 paradigm(VoicePicker inline + tts_models.json + pydantic validation + GSV 2 mode schema 预留 + adding-new-tts-model playbook)。**全段已 ship 并真机验证**。
**INV-12** 系列(`docs/INV-12-fish-config-audit.md`)2026-05-25 · Fish TTS user_override 配置层 audit + Stage 2 backend ship(reference_audio + reference_text + fish_temperature 配对约束 + 4 endpoints)。
**INV-13** 系列(`docs/INV-13-proactive-shortterm-pollution-audit.md`)2026-05-26/27 · proactive → short_term ja-format 污染 audit 3 轮 + Option F+G ship · §11 root cause 推 Option D 误诊 → §12 DB 定量反驳(conv=62 20-turn JA streak counter-example + proactive 100% ja compliance)→ §13 ship F(ja-aware 段条件注入)+ G(软化字数硬约束)+ main.py:484 deprecation comment;Lesson #16/#17/#18 沉淀。
**INV-14** 系列(`docs/INV-14-vad-disappeared-audit.md`)2026-05-27 · "VAD 不见了" audit + ship + 部分 revert · 真因主要是 backend HF_HUB_OFFLINE 硬编码导致 whisper preload 29/29 失败(VAD 代码完整未被删)· §7 ship `c2d8924`(P1 backend fix 保留)+ `3f24d6c`(P2 UI re-expose 后即 revert `aed67cc` · PM 真机 verify VAD UI 一直在 Capabilities → ASR tab · 主设置加冗余);**Lesson #19 修正**:"用户找不到旧入口" ≠ "入口缺失" · audit 前先 mental walkthrough visibility verify · 别凭 grep import 推入口失踪。ROADMAP P2 backlog 标 ✅(P1 修法生效)。
**INV-15** 系列(`docs/INV-15-tts-gap-and-vad-stuck-audit.md`)2026-05-27 · audio chunk gap + VAD 卡空闲 audit + P1 + P2 ship · 真因(a) = TTS consumer FIFO HOL blocking on cosyvoice cloud 偶发 9s outlier(chunk 15 closed 时假设破);真因(b) = MediaStream stale 无 recovery + threshold 65 太高;§8 ship P1 `534a6ca`(`merge_short_sentences` 扩 zh);§9 ship P2 `8537393`(三件套:Option H 健康检查 + Option I onended recovery + Option G diagnostic UI);Lessons #20/#21/#22 沉淀。

---

## 全部刀次（按时间倒序）

| 日期 | 主题 | 一句话结论 | 位置 |
|---|---|---|---|
| 2026-05-27 | **INV-15 P2 三件套 ship**(commit `8537393`) | mic stream stale recovery + diagnostic UI 复合 ship 单 commit。Option H 健康检查 · `useAudio.initStream` 拆 `createAudioGraph` / `teardownAudioGraph` / `isAudioGraphHealthy` 三 helper · verify `MediaStreamTrack.readyState === 'live'` + `AudioContext.state !== 'closed'` · 不健康 teardown 重建 · suspended → resume() 优先;Option I `MediaStreamTrack.onended` listener + vadLoop 周期 frame check 兜底(60 帧 ~1s)· `recoverStream()` 单飞行锁 + sleep 时尊重用户不偷偷重申 mic · permission denied → `setVadState('sleep')`;Option G store 加 `vadCurrentMax` + setter · vadLoop 每帧写 · VadBar 从纯彩条 → progress bar + threshold marker(虚线竖条)+ 数字 readout "now: X / threshold: Y"(等宽 tabular-nums)· 超 threshold 换 accent 色;Lesson #22 沉淀(实时 input device API 三件套缺一不可);tsc clean / 0 regression;PM 真机预期场景 A/B/C/D(正常/切 IDE/系统抢 mic/WS 重连)VAD 自动 recover · 不需 reload | INV-15 §9 + commit 8537393 |
| 2026-05-27 | **INV-15 P1 Option A ship**(commit `534a6ca`) | `merge_short_sentences` 扩到 zh · 删 `ws.py:861` + `engine.py:453` `if tts_language in ("ja","en")` gate · merge 对所有 tts_language 启用(zh/ja/en);Sanity 5 zh 短句 → 3 merged(-40%)· PM 15:12 实际 turn lengths(7/11/12/25 chars)default short_threshold=8 下 merge 不一定触发 · 若不够升级 P2 threshold tune(8→12 + 15→25);0 regression(merge_short_sentences ja/en 行为不变);Lessons #20(close 假设 long-running verify)+ #21(enable gate 语义不用语种白名单)沉淀;ROADMAP 句子并发 backlog 加 reference(部分 mitigation · 不是完整复活);**与 INV-13/14 完全无关**(F+G 仅 proactive trigger / 14 是 ASR+UI · 都不动 TTS pipeline) | INV-15 §8 + commit 534a6ca |
| 2026-05-27 | **INV-15 audit · audio chunk gap + VAD 卡空闲**(2 issues 联合 audit) | PM 真机 15:12 turn 实测 4 sentence yields + TTS times 4475/4356/9030/3505ms(`#3 9s outlier`)· consumer FIFO HOL blocking 5s 沉默;chunk 15(2026-05-16)closed 时假设 "TTS 单句合成时间稳定" 被实测打破 · cosyvoice cloud stochastic latency · PM 关 VPN 关 冷启仍体感;PM 切 cosyvoice zh 后 `merge_short_sentences` ja/en-only gate 失效放大;VAD 卡空闲推测 threshold 65(raw 166/255)太高 · default 50(raw 127)即可压过正常说话(120-180 max);`initStream` 缺 stream 健康检查(MediaStreamTrack readyState / AudioContext state)无 stale-stream recovery;**INV-13/14 改动 explicit 无关**(F+G 仅 proactive · 14 仅 ASR+UI);推荐 P1 Option A(merge 扩 zh · 10 min)+ Option K(PM 调 threshold 50 · 0 code);P2 升级:VAD UI 实时 max bar / stream 健康检查 / `MediaStreamTrack.onended` listener;P3:TTS_CONCURRENCY 3→5 / push_latency observability(ROADMAP:206 现有 backlog);**不推荐** out-of-order playback / 整 turn 预 buffer | INV-15 §1-§7 |
| 2026-05-27 | **INV-14 P2 revert · UI re-expose 冗余 PM verify 后撤销**(commit `aed67cc`) | PM 看截图后 verify VAD UI 一直在 ⚙ Capabilities → AI Providers → ASR tab(深 3 层 · 自 bugfix-2.2 SettingsPanelV2 收口后未变)· 主设置加 "🎤 语音输入" 4 号 section 是冗余;P2 commit `3f24d6c` revert · SettingsPanelV2 回 10 section;**Lesson #19 修正**:audit "UI 失踪" 类问题前必须 mental walkthrough · 不仅 grep import · "用户找不到旧入口" ≠ "入口缺失" 是不同问题(visibility vs 入口);INV-14 §7.8 reflection 写入 audit doc · ROADMAP P2 entry 更新(P1 保留 / P2 reverted);P1 backend ASR fix(`c2d8924`)保留 · 这是真 broken | INV-14 §7.8 + commit aed67cc |
| 2026-05-27 | **INV-14 VAD 恢复 ship**(2 commits 拆分 · P1 backend + P2 UI · P2 后即 revert) | `c2d8924` backend ASR fix · 删 `backend/main.py:21-22` 硬编码 `HF_HUB_OFFLINE=1` + 配套 `.env` HF_HUB_OFFLINE=0 / TRANSFORMERS_OFFLINE=0 / 拆开 line 11 parse bug(stale MCP token 丢)· 预期 backend restart 后 faster-whisper 首次 download `Systran/faster-whisper-small` ~80MB 到 `~/.cache/huggingface/hub/` · cached 后即便 OFFLINE 也能 load;~~`3f24d6c` UI re-expose~~(reverted as 冗余 · 见上一行);docs:ROADMAP P2 backlog 标 ✅ · LESSONS #19 沉淀(后修正)· INV-14 §7 ship 记录;Sanity 通过(`import backend.main` HF_HUB_OFFLINE = '0' 验证 · frontend tsc 干净)· 真机回归 PM restart 后看 whisper preload 成功 log + VAD 录音 → asr_result fire | INV-14 §7 + commits c2d8924/aed67cc |
| 2026-05-27 | **INV-14 VAD 监听消失 audit**(3 个独立问题 verify) | PM 反馈 "VAD 不见了" · 扩 grep + log 实证:VAD 代码 100% 完整(useAudio.ts 128-224 / VadBar / recordingMode state 都在 · 自 2026-05-04 v3-WIP baseline 起未删/未禁)· 表象由 2 层叠加问题造成:层 1 backend ASR broken(`backend/main.py:21-22` 硬编码 `HF_HUB_OFFLINE=1` + `~/.cache/huggingface/hub/` 无 whisper-small snapshot → preload 29/29 失败 / asr_result 0 次);层 2 主设置 UI 入口失踪(bugfix-2.2 SettingsPanelV2 收口 10 section 漏 include `AsrVadSection` · 唯一入口在 Capabilities → ASR tab);ROADMAP `ROADMAP.md:38` P2 backlog 已记此 issue 但未 fix;`.env` `TRANSFORMERS_OFFLINE=1MCP_BEARER_TOKEN=...` 同行拼接 parse bug 同期发现;推荐 P1 backend HF_HUB_OFFLINE 修(Option A · ~5 min)+ P2 UI re-expose(~10-15 min);PM 拍板 ship | INV-14 audit §1-§6 |
| 2026-05-27 | **INV-13 Option F + G + main.py:484 deprecation comment ship**(3 commits 拆分) | `0b38cd6` Option F · trigger prompts 加 ja-aware 段(条件注入 · tts_language != zh 才附 · zh 角色零额外 token)· 3 helpers + 3 文件改;`659351e` Option G · 软化 trigger 字数硬约束("8-15 字硬约束 ⚠️⚠️⚠️" → "**简短问候式**(参考 8-15 字 · 软指引)⚠️"+ "40-80 字硬要求" → "**适度长度 · 1-2 句**(参考 40-80 字 · 软指引)")· 3 文件;(本 commit · §13 docs)`main.py:484` 加 deprecation comment 标 §12.7 真意 + LESSONS #16-18 + INV-13 §13 ship 记录 + INVESTIGATION-INDEX 同步;Sanity 实测:zh 角色 prompt 软化 + 零 ja-block(456-461 chars · -hard_8_15 -triple_warn +soft_措辞);ja 角色 +266 chars ja-block(688-727 chars);后续 PM 真机 1 周内无 thinking leak / ja drop regression 则 INV-13 整段 closed | INV-13 §13 + commits 0b38cd6/659351e + (C3) |
| 2026-05-27 | **INV-13 §12 第三轮 audit · "聊着聊着被带坏" 现象 verify**(PM challenge 数据反驳) | 扩大 sample 到全部 56 cid=1 assistant turn(§11 14 turn 加 42)· per-conversation ja compliance 统计 · **conv=62 20 连续 JA turn**(2.5h · 含 4 proactive)实证 LLM 在 stable voice_model + ja directive 下不 drift;**conv=61 id=29→30 阶跃 flip 真因 = v4_0_0_mai_revert_zh migration pre-hotfix bug**(每次 restart 把 cid=1 revert 回 cosyvoice zh · `fd11d74` 已闭合 with provider scope guard);renderer log 实证 tts_lang 振荡(2026-05-25 06:44-10:46 zh / 11:03+ ja · stable_chars 9322→11749 = ja directive 上线);**§11 Option D drop 决策正确**(post-restart 第一 turn 立即恢复 ja · strip 无害);Lesson #16-#18 沉淀候选;PM "聊着聊着被带坏" 现象 = migration revert bug 表象 · 不是 LLM drift 或 short_term 污染 · 已闭合 | INV-13 §12 |
| 2026-05-26 | **INV-13 §11 第二轮 audit + §1-§10 first audit · Proactive → short_term ja-format 污染 audit**(PM challenge 后扩大 audit) | §1 proactive engine 实施 trace · §2 short_term read/write 路径 · §3 ja directive 注入对比 chat vs proactive(verify ja directive 都通过 Layer A 同源注入 · proactive 不缺 directive)· §4 DB 14 turn sample 实证(proactive 100% ja / normal 90%)· §5 Fix Options A-F evaluated · §6 推荐 Option D(main.py:484 strip ja gate)+ Option F(trigger prompts ja-aware);§11 extended audit per PM challenge · grep verify "8-15/40-80 字" 真实性(确实存在 · PM 2 周前 commit `9e4d1dd`/`20cc231`/`da3ac588` author)· §11.4 数据 reanalyze 显示 Option D 前提不成立 → **drop Option D**;§11.5 dinner_call id=118 thinking leak 是多约束冲突(Layer A ja ≥10字 ↔ Layer D trigger 8-15字 ↔ Layer B PROACTIVE 50字 ↔ "no metadata" instruction);refined Fix = Option F + G + H(skip_short_term 扩展);§11.7 wake_call 已 `skip_short_term=True` 现成参考 | INV-13 §1-§11 |
| 2026-05-26 | **INV-11 Stage 1.5 followup ship**(plus label fix + section headers + tts_models.json + GSV 2 mode + adding-new-tts-model playbook + Lesson #13/#14/#15) | `cosyvoice-v3.5-plus` label 修正为 "系统音 + 复刻双轨"(Lesson #13);voice list 系统音/复刻分双 section header(showHeaders = both non-empty);新建 `backend/config/tts_models.json`(3 provider · 4 model · 含 mai_v4 GSV 完整 schema + mode="trained" 字段);`backend/tts/registry.py` 重写走 pydantic `ModelSpec/ProviderSpec/TtsModelsConfig`(`extra="allow"`) · 启动 fail-fast on schema 损坏 + missing-file `_hardcoded_fallback()`;GSV mode 字段加 schema(缺省视为 "trained" 向后兼容)· `list_voices` zeroshot 分支返 placeholder `requires_reference_upload=true` + `gsv_mode="zeroshot"`(frontend ref upload UI 待 future);新建 `docs/adding-new-tts-model.md`(3 例:GSV trained yae_v1 全流程 / GSV zeroshot placeholder / Fish 新 model + schema 字段表 + 排错);hotfix `VoicePicker buildJsonFor` cosyvoice 分支补 `model` 字段(原 `buildVoiceModelJson` 不带 model · plus→flash→plus 单向卡死回 flash · 现 _parseVm 直接读 model 不再 infer);Lesson #13/#14/#15 沉淀;tsc + backend smoke clean | INV-11 Stage 1.5 followup |
| 2026-05-26 | **INV-11 Stage 1.5 ship**(全角色 provider × model × voice paradigm) | `backend/tts/registry.py` 新建 hardcoded `_GSV_MODELS / _FISH_MODELS / _COSYVOICE_MODELS` 注册表 + `list_providers/list_models/list_voices/get_provider_tree/build_voice_model_json` API + `_load_cloned_voices_from_db()` 从 `characters.voice_model` 抽 `cosyvoice-v3.5-plus-bailian-*` 复刻 voice;`GET /api/tts/providers` 新 endpoint 返 nested tree(provider × model × voice 一次性 fetch);前端 `components/VoicePickerModal.tsx` 3-step dropdown(provider / model / voice)+ CharacterPanel +20 onSave callback;PM 真机 cid=1/3/4/101 cosyvoice ↔ gsv 切换 verify ✓;**paradigm B 后续**:VoicePickerModal → `character/VoicePicker.tsx` inline(modal wrapper 删 + 删 [📢 试听并选 voice] trigger button + dropdown change auto-save debounce 300ms PATCH /api/characters/{cid})· Lesson INV-11 #12 沉淀(paradigm 完整后 inline 体验更好) | INV-11 Stage 1.5 |
| 2026-05-25 | **INV-11 Stage 1 GSV 真接入 ship**(mai_v4 emotion-aware ja TTS) | `backend/tts/gsv.py` 新 GSVTTS(TTSBase) provider · `/set_gpt_weights` + `/set_sovits_weights` lazy-init per-process once-per-key + `/tts` synthesis + emotion bank 16 ref wav LLM emotion 路由 + local lab cache(`tts/gsv/mai_v4/*.lab`)+ `_resolve_weights_field(gpt_weights/gpt_path)` 字段名 fallback(Lesson #11 backward compat 阶段化);`backend/tts/__init__.py` `_build_engine` gsv 分支 voice_model_json 透传 GSVTTS · 不污染 VoiceConfig;DB cid=1 Mai voice_model UPDATE → gsv mai_v4 完整 schema(server_url / weights / emotion_bank_dir / inference_params);PM 真机 chat A/B 仍 work(cosyvoice longyumi_v3 ↔ gsv mai_v4 切换) | INV-11 Stage 1 |
| 2026-05-25 | **INV-11 Stage -1 prompt experiment**(V3 success) | Layer A1 ja directive 配 GSV emotion bank 16 emotion 输出格式 → LLM emotion 输出与 GSV emotion bank ref wav 1:1 对齐 · V3 prompt 实证 work | docs/INV-11-stage-minus1-prompt-experiment.md |
| 2026-05-25 | **INV-11 Stage 0 LLM output audit** | audit LLM 输出现状 + GSV 接入前 layer A directive 形态(为 Stage -1 prompt 实验 baseline) | docs/INV-11-stage0-llm-output-audit.md |
| 2026-05-25 | **INV-12 Stage 2 backend ship**(Fish TTS user_override 层) | `characters.voice_model` 加 user_override 字段(reference_audio_path + reference_text + fish_temperature · 配对约束 + 4 endpoints);Fish s2-pro per-character reference upload paradigm | docs/INV-12-fish-config-audit.md |
| 2026-05-22 | **INV-10 §1+§2 · v4.0 立绘馆 voice greeting feature ship**(独立主线 · Phase 2 closed 后)| backend `2b597bc`:new DB table character_voice_lines(id / character_id FK CASCADE / audio_path / text_description / language / duration_ms / created_at + INDEX)+ `backend/routes/voice_lines.py` 4 endpoints(POST multipart upload 415/413 validate + UUID filename + duration extract / GET list / GET random 404 empty / DELETE file+row)+ `backend/main.py` lifespan 1b34 migrate + router register + StaticFiles mount /static/voice_lines(项目首次 StaticFiles)+ `tests/test_voice_lines.py` 31/31 PASS · `scripts/seed_mai_voice_lines.py` 6 Mai WAV from sweep 输出 seed to cid=101(canon range markers ja 1.5-6.5s);frontend(本 commit)`lib/voice_lines.ts` API client(playRandomVoiceGreeting 主路径)+ `CharacterDetailModal.tsx` mount useEffect fetch random + play + unmount cleanup + `VoiceLinesSection.tsx` 列表 / 上传 / 预览 / 删除 + `CharacterPanel.tsx` 集成(form edit mode + form.id 时 render);Lesson INV-10 #1 沉淀(Fish s2-pro WAV header n_frames=INT_MAX bug · fix 用 file_size/bytes_per_sec 推算绕过 mutagen + wave.getnframes,mp3/ogg 仍走 mutagen bitrate)· 类比 INV-9 #6 SDK 字段表 ground truth · audio container 层"信任校准";真机验收 ready · 打开立绘馆点 Mai → 6 seed Mai canon range ja audio 随机播 | INVESTIGATION-10.md |
| 2026-05-22 | **INV-9 §7 · Phase 2 第 7 / 收尾 commit · cost cap + profile_data JSON + cid=101 fish lock ship**(Phase 2 整段 closed)| per INV-8 §1.3.6 决策 5 重写 + Q7 + PM §7 lock;`backend/utils/cost_estimator.py` 新建 180 行(`FISH_S2_PRO_COST_PER_M_BYTES_USD=15.0` + default cap $1/day · $20/month + `estimate_fish_cost_for_text` byte-based / `_for_chars` lang-aware ja/zh 3 bytes/en 1 byte + `get_user_cost_caps` profile_data JSON 读 + DB 聚合 `get_today_fish_cost_usd` / `get_month_fish_cost_usd` + `check_fish_cost_cap_exceeded` 主入口);`backend/observability/tts_log.py` `estimate_cost` 加 fish family 路径(s2-pro/s1/v1.6 → byte-based · `raw_text` 精确 / `input_chars × 3` 兜底)+ `log_tts_call` 传 raw_text;`backend/routes/ws.py` `_handle_message` 读 voice_model 后 + `get_tts_engine` 前加 fish cap check;触达 → `voice_model=None` fallback yaml default CosyVoice + `ws.send_json({"type":"tts_cost_cap_exceeded",...})` 前端 toast;`frontend/src/hooks/useWebSocket.ts` `WsMessage` 加 5 字段 + `case 'tts_cost_cap_exceeded'` handler 调 `pushNotification`;new `tests/test_cost_estimator.py` **34 cases**(7 fish estimate 单元 + 6 get_caps + 5 estimate_cost tts_log 桥接 + 6 集成 DB 聚合 + 6 集成 profile_data 极低 cap 触发 daily cap → exceeded=True 实证);**DB UPDATE characters.id=101 voice_model**(per PM 听感 lock):provider="fish" / voice="mai5min_0033" / model="s2-pro" / tts_language="ja" / reference_audio_path + reference_text / **fish_temperature=0.2** lock,其它 fish_* default 不强制覆盖 SDK(per PM);**345/345 cases PASS / 0 regression**(test_cost_estimator 34 + test_fish_provider 37 + test_fish_marker_isolation 35 + test_sanitize_ja 32 + test_text_filters_ja_whitelist 38 + test_tts CosyVoice/Edge/SoVITS legacy 106 + test_bugfix_4_observability 31 + test_tts_final_guard 32);DB backup `momoos.db.backup_inv9_§7_20260522_*` 已存;2 lesson 沉淀(INV-9 #9 cap fail-safe 偏放行原则 · 辅助治理路径失败不阻断主链;类比 INV-7 §1.7 #10 / INV-9 #10 estimate_cost signature 加 optional kwarg 模式 · 度量函数升级 backward-compat 渐进采用;类比 INV-9 #3);**Phase 2 整段 closed**(7 commits + 1 DB UPDATE);ROADMAP v4.1+ 多 provider 扩展刀 backlog 追加(per PM 2026-05-22 重定向 · GPU 远程 inference `RemoteFishTTS(TTSBase)` FastAPI wrap + GSV (GPT-SoVITS) `GSVTTS(TTSBase)` 2 sub-anchors · reference b34ad70 audit + Lesson #8 + mai5min/ 5min 素材);INV-9 part 3 末尾追加 PM 后续决策 audit 留档不再扩注;下一里程碑 = Phase 3 流式管线 + H3 fix + Step 6 instrumentation(per INV-8 §1.收口.4 backlog) | INVESTIGATION-9.md §7 + part 3.7 |
| 2026-05-22 | **INV-9 中插 part 3 · Phase 2 第 6 commit · Part 1 vs Part 2 diff audit + Part 1 repro ship**(stochastic verdict 定案)| PM 听后确认 Part 1 T=0.2 质量明显好于 Part 2 T=0.20 三 runs → audit 两脚本 diff + repro Part 1 验服务器稳定性;**diff verdict**:仅 1 实质差异 = voice_model JSON 是否含 `"fish_seed": 42`(其它参数透传/ref 加载/TTSRequest 构造/编码/import 全完全相同);SDK 不识 seed(per INV-9 #6),两路径 TTSRequest payload 在 SDK 端等价;**repro verdict**:`scripts/fish_param_repro.py` 用与 Part 1 完全相同 build_voice_model(含 fish_seed=42)重跑 4 calls T=0.2,**0/4 md5/bytes/duration match**(bytes Δ S1+49k / S2-57k / S3+25k / S4+29k);**audit 结论**:Fish 服务器侧每次 TTS call 独立采样,Part 1 vs Part 2 听感差异 = **服务器侧抽样运气**,**不是脚本逻辑差**;无脚本侧 fix(per INV-9 #6/#7 stochastic 实证一致);4 mitigation options(M0 接受 stochastic CC leaning / M1 切 reference_id 预上传未实证 / M2 retry+scorer 工程爆炸 / M3 等 SDK 加 seed future hook 已保留);新建 `scripts/fish_param_repro.py` + 4 WAV `INV9_repro_T02_S{1-4}.wav` + summary.json;0 backend 代码改动;1 lesson(INV-9 #8 复现失败作为 audit 工具的双重价值 — 先 audit diff + repro 同条件,后改参数;Fish s2-pro 输出 inherent stochastic 默认参数选择应基于多 batch 听感分布);**part 3 closed**,等 PM 拍板默认 T(基于 Part 1 + Part 2 + repro 共 20+ T=0.2 多 batch 听感分布)→ 进 §7 收尾刀 | INVESTIGATION-9.md 中插 part 3 |
| 2026-05-22 | **INV-9 中插 part 2 · Phase 2 第 5 commit · narrow window + T=0.20 变异度 ship**(PM 听 part 1 后 T=0.2 倾向最优,要邻域多探 + 变异度)| `scripts/fish_param_sweep_part2.py` 新建 250 行;T=0.20 × 4 texts × 3 runs(12 calls 变异度主目的)+ T=0.15 × 4 texts × 1 run + T=0.30 × 4 texts × 1 run = 20 calls;**20/20 OK**;**T=0.20 变异度实测 6.7%-27.6% bytes range**(S3 27.6% 最大 / S4 6.7% 最小;短 text 采样随机性放大 / 长 text inherent stochastic dilutes)+ md5 3/3 全不同实证 SDK stochastic sampling 无 deterministic 控制(per part 1 seed NON-FUNCTIONAL);跨 T audio_dur 跟 T020 三 runs variance 同量级 → **T 维度影响 ≈ inherent noise floor**(0.15-0.30 narrow window 内);balance API 延迟刷新(本次 0 delta · package batch update 滞后,生产 cost 监控不依赖此 API · 走本地 char→bytes 累计 per 决策 5);20 WAV 输出 `INV9_param2_T020_S*_run*` / `T015_S*` / `T030_S*` + summary part2.json;0 backend 代码改动(参数字段 part 1 已加,本刀纯加脚本);0 regression;1 lesson(INV-9 #7 stochastic sampling 参数 sweep 解释力边界:T 维度信号被 inherent stochastic noise 淹没 → 默认 T 选择主要看 PM 听感而非 audio_dur 量化);**Phase 2 中插刀整段 closed**(part 1 19 + part 2 20 = 39 WAV);等 PM 听 part 2 20 WAV 拍板 final 默认 T → schema 默认值锁,进 §7 收尾刀 | INVESTIGATION-9.md 中插 part 2 |
| 2026-05-22 | **INV-9 中插 · Phase 2 第 4 commit · Fish 参数 sweep 刀 ship**(Mai fidelity 调优)| PM 听 INV9_smoke 反馈不够像 Mai → temperature × text grid 实验;`backend/tts/voice_config.py` 加 3 Optional 字段 fish_temperature / fish_top_p / fish_seed(parse 不 raise · backward compat)+ `backend/tts/fish.py` `_build_request` if not None 透传 TTSRequest(未配 = SDK 真默认对照组)+ fish_seed 保留 future hook 构造 log warning(SDK TTSRequest 字段表实测无 seed);new `scripts/fish_param_sweep.py` 16 主表 grid(temp ∈ {0.2/0.4/0.6/0.8} × texts S1-S4 含 Mai 自介 canon "私、桜島麻衣...")+ 3 seed sanity runs(T=0.4 S2 seed=42 × 3)+ balance check + summary.json + 视觉表格;19/19 calls OK;**seed param NON-FUNCTIONAL** 实证(3 runs 全不同 md5 / 不同 bytes,SDK 1.3.0 `TTSRequest.model_fields` introspect 无 seed);实测 cost ~$0.028 vs 估 $0.50(~5.6%,生产 cost cap 设计可放宽);**248/248 邻近 cases 全绿 / 0 regression**(test_fish_provider 37 + test_fish_marker_isolation 35 + test_sanitize_ja 32 + test_text_filters_ja_whitelist 38 + test_tts 106);19 WAV outputs(`scripts/fish_probe_outputs/INV9_param_T{02-08}_S{1-4}.wav` + `T04_S2_run{1-3}.wav`)给 PM 听感拍板默认参数;1 lesson 沉淀(INV-9 #6 SDK 字段表是真实接受参数集 ground truth);**中插刀 closed**,等 PM 听 19 WAV 拍板默认 → 进 §7 收尾刀(cost cap + profile_data JSON)| INVESTIGATION-9.md 中插 |
| 2026-05-22 | **INV-9 §5+§6 · Phase 2 第 3 commit · per-provider [bracket] markers 双重隔离 ship**(Hard Req 原子化 + β inline final lock + Mai canon range)| §5 生成端 LLM `backend/agents/prompt/templates/layer_a.j2` ja directive 加 `{% if voice_provider == 'fish' %}` 50+ 行 Jinja 子分支教 Mai canon range markers(冷静档 composed/calm/deadpan / 挖苦档 teasing/sarcastic/dry tone / 温柔档 soft chuckle/gentle/soft voice / 罕见档 mildly surprised/mild embarrassment / Pause short/pause/long;明确禁用超 range:excited/shouting/screaming/laughing loudly + 激动大叫语义)+ 含 ✓/✗ 3 类示范;`renderer.py` + `chat.py` 加 voice_provider 参数透传(8 行);§6 接收端 `backend/utils/text_filters.py` 新增 `_FISH_EMOTION_MARKER_RE` + `strip_fish_emotion_markers` + `strip_ja_en_tags_for_subtitle` 链尾追加(字幕跨 provider 一律剥)/ `backend/tts/__init__.py` `_PreprocessingEngine` 加 provider 分流(fish pass-through / non-fish strip)+ **关键 fix** 拆 `_LEGACY_BRACKET_NOTATION_RE` 出 `_PREPROCESS_PATTERNS` + `preprocess_tts_text(text, strip_bracket_notation: bool)` opt-in(否则 fish markers 被 v3-F `[stage direction]` 历史 regex 误剥 — test 暴露);new `tests/test_fish_marker_isolation.py` 35 cases(strip_fish_emotion_markers 单元 8 + _PreprocessingEngine 分流 5 + e2e 2 + 字幕 3 + layer_a.j2 渲染 3 子);new `scripts/fish_marker_e2e_smoke.py` Mai canon range 5 markers 真合成(composed/sarcastic/teasing/gentle/soft chuckle 各跑 1.58-2.42s WAV)+ cosyvoice mock 5/5 剥除 verify + subtitle 5/5 剥除;**337/337 cases PASS / 0 regression**(本 commit 35 NEW + test_sanitize_ja 32 + test_text_filters_ja_whitelist 38 + test_tts_final_guard 32 + test_tts_strip_fallback 57 + test_tts 106 + test_fish_provider 37);2 lesson 沉淀(INV-9 #4 preprocess 与新 marker 语义冲突需 per-feature opt-in / #5 Hard Req 双重隔离两端原子化必要性);**Phase 2 §5+§6 closed**,下刀 = §7 cost cap + profile_data JSON(Phase 2 收尾;§8 cid=1→101 数据迁移取消 per PM 2026-05-22) | INVESTIGATION-9.md §5+§6 |
| 2026-05-22 | **INV-9 §2+§3+§4 · Phase 2 第 2 commit · TTS 抽象层 + Fish provider ship**(合刀 §2 太小 + §3/§4 互依)| `backend/tts/voice_config.py` VoiceConfig 加 4 字段(tts_language / reference_audio_path / reference_text / fish_latency)+ parse_voice_config fish 分支 raise validation(mode_A only · 不静默 fallback)/ `backend/tts/__init__.py` _build_engine 加 fish 分支(7 行延迟 import)/ `backend/tts/fish.py` 新建 201 行(FishTTS(TTSBase) 实现 mode_A only zero-shot:构造一次性读 reference_audio bytes cached + API key resolve env > api_key.txt > 空 + warning;synthesize → bytes 通过 asyncio.to_thread 包 SDK sync collect;backend='s2-pro' lock + latency='balanced' lock;HttpCodeErr / 异常全 catch + log_tts_call INSERT);synthesize_stream 留 Phase 3 H3 fix 合刀;new `tests/test_fish_provider.py` 37 cases(VoiceConfig 默认 / 显式 / parse fish raise 缺 ref 4 case / parse fish 全字段 / parse cosyvoice backward compat 9 char DB 矩阵 / _build_engine 工厂分流 3 子);new `scripts/fish_provider_smoke.py` 直调 smoke 跑通 Mai 普通日语 2.79s WAV + `[soft chuckle]` marker 2.18s WAV(emotion β inline `[bracket]` PM 听完确认 work);**302/302 cases PASS / 0 regression**(test_sanitize_ja 32 + test_text_filters_ja_whitelist 38 + test_tts_final_guard 32 + test_tts_strip_fallback 57 + test_tts 106 cosyvoice/edge/sovits legacy + test_fish_provider 37);2 lesson 沉淀(INV-9 #2 延迟 import 隔离 SDK 依赖 / #3 mode_A validation parse 阶段 raise 不静默 fallback);**Phase 2 §2+§3+§4 closed**;下刀 = §5+§6 合刀(Hard Req per-provider 双重隔离 · LLM 端 layer_a.j2 fish 子分支教 markers + 后端 sanitize 新增 strip_fish_emotion_markers)| INVESTIGATION-9.md §2+§3+§4 |
| 2026-05-22 | **INV-9 §1 · Phase 2 第 1 commit · sanitize A1 fix ship**(per INV-8 §1.收口.6 Q1 lock)| `extract_tts_text` 重写:ja/en 路径半截 `<ja>/<en>` fallback skip(PM bug #1 fix)/ zh 路径显式剥 `<ja>/<en>` 整段 + 兜底半截 skip(PM bug #2 fix);新增 `_has_unclosed_ja_en_tag` helper + `_PARTIAL_JA_EN_OPEN_RE` regex;新建 `tests/test_sanitize_ja.py` 10 test functions / 32 assertions(6 主 case + backward compat + edge + helper 行为锁)全 PASS;**邻近 3 test suite 回归 127 case 全绿**(test_text_filters_ja_whitelist 38 / test_tts_final_guard 32 / test_tts_strip_fallback 57);合计 159/159 PASS / 0 regression;改动量 +254 / -10 行;**backward compat 保留**(LLM 真无 tag 漏标整段仍走 fallback,与 NEW skip 分支区分);Lesson INV-9 #1 沉淀(NEW skip vs 原 fallback 双层 invariant);**Phase 2 §1 closed**,下刀 = Phase 2 §2 voice_config 4 字段 + fish provider raise validation | INVESTIGATION-9.md §1 |
| 2026-05-22 | **INV-8 §1 · TTS 模块化 + Fish 集成 Phase 1 audit closed**(5 节 + sanitize bug audit + 7-step plan)| Phase 1 纯只读 audit 闭环;§1.1 句间停顿 4 假设代码侧定 verdict(H3 前端 +1000ms safety margin 几乎确定主因,代码注释明示)+ 11 log 点 instrumentation 提案(挪 Phase 3 H3 fix 合刀);§1.2 TTS 链路 6 文件树 + 8 step 文字图 + 3 抽象插点(A factory + C VoiceConfig 联合 Phase 2 / B 流式 Phase 3);§1.3 Fish API 调研 stage 1+2(`scripts/fish_probe_T1_T6.py` 13 call 全跑通 + 12 WAV outputs)— `references[]` 非必需(default voice 路径)/ TTFA balanced 593ms 快 ~3-4x normal / balance API 实有(`get_api_credit` + `get_package`,docs 缺漏)/ s2-pro emotion `[bracket]` 自然语言不限固定集;§1.4 9 char DB 矩阵 + cid=101 樱岛麻衣三表完整(零代码改 + 1 字段 flip 复活)+ ja 链状态修正(不是休眠是待机,config-driven dormancy)+ 唯一显式语种路由 = layer_a.j2:32-83 ~42 行 Jinja directive;§1.5 sanitize bug audit(13 子项 `scripts/inv8_sanitize_audit.py`)— **bug #1 部分修(A5 半截 + A6.1 错标残留)+ bug #2 未修(切 zh 含 `<ja>` 完整保留)** + 历史定位回退 commit `0e079a4` + Option A1 lock(fix sanitize + 沿用 `<ja>`)+ Hard Req per-provider 双重隔离;**5 决策最终三档**:1 ⚠️(沿 `<ja>` 隐式 display_zh)/ 2 ✅ / 3 ⚠️(`synthesize` + 新增 `synthesize_stream`)/ 4 ⚠️(β inline `[bracket]` 待 WAV final lock)/ 5 🔁(本地 cost + per-user cap + balance API 实有);4 lesson 沉淀(#1 docs 红 flag 描述偏差 / #2 docs 缺漏 SDK 是真源 / #3 sanitize sub-language path 对称性 / #4 LLM 行为 ↔ sanitize ↔ provider 三层分工);Phase 2 拍板清单 Q1-Q7 + Step 6 instrumentation 挪 Phase 3 合刀 backlog | INVESTIGATION-8.md §1 |
| 2026-05-21 | **INV-7 §2 · 子轨 B P1.netease 13→2 双 dispatcher 折叠 ship + 子轨 B closure**(第 6 刀,模板复用 #3,**收尾刀**) | 单 commit ship;netease_music.py 7 旧 cap → 1 `netease_web` dispatcher + 7 `_handle_*` internal + 3 类必填校验(keyword / playlist_id / title);netease_playback.py 6 旧 cap → 1 `netease_local` dispatcher + 6 `_handle_*` internal + 2 类必填校验(song_id / playlist_id);tool_addendum.py 11 处引导文 web/local 路径区分微改;frontend tool_labels.ts 单 `'netease.'` 拆 2 prefix(`netease_web` "查歌单…" + `netease_local` "本地播放…"differentiated UX);3 smoke 全过(双 dispatcher 注册 + 14 query 含边界 case 路径选择 13/14 命中 + dispatcher routing 9 档);**实测 -1,136 token (P1.bilibili 8,437 → 7,301 / 44.9% 累计 / 子轨 B 终态)**,与估算 ~1,180 吻合(差 3.7%);lesson #10 capability-tag fallback regex 接受 trade-off backlog;**子轨 B 整轮 5 commit closed,累计 -5,949 token / -44.9%**(§ 收尾归档 B.1-B.5) | INVESTIGATION-7.md §2 + § 收尾归档 |
| 2026-05-21 | **INV-7 §1 · 子轨 B P1.bilibili 11→1 入口折叠 ship**(第 5 刀,模板复用 #2,**最大头**) | 2-stage(Phase 1 INV-6 封存 + Phase 2 audit/落代码)单 commit ship;bilibili.py 11 旧 cap → 1 `bilibili` dispatcher + 11 `_handle_*` internal + 5 类必填校验(keyword/mid/bvid-or-aid);tool_addendum.py 8 处引导微改;**frontend tool_labels.ts retro-fix 3 prefix**(media + apple_calendar + bilibili 同款 fold-后失 mapping 补漏,需 yarn build 才生效);3 smoke 全过(ToolRegistry / 11 LLM query 100% 调 bilibili / 6 档 dispatcher routing);**实测 -1,169 token (P1.apple_calendar 9,606 → 8,437 / 36.3% 累计)**,超 PM 预期 ~900 中位数 ~30%;lesson #9 三 grep 模式(cap-name + module import + frontend startsWith)记入,后续 P1.netease 必走 | INVESTIGATION-7.md §1 |
| 2026-05-21 | **INV-6 封存(915 行) + INV-7 起用** | INV-6 完整覆盖子轨 B 实施前 3 刀(§1 P3 / §2 P1.media / §3 P1.apple_calendar)+ dispatcher 模板 6 要点 + lesson #7/#8;后续实施收尾(P1.bilibili + P1.netease)迁 INV-7 | INVESTIGATION-INDEX.md + INVESTIGATION-6.md §封存说明 |
| 2026-05-21 | **INV-6 §3 · 子轨 B P1.apple_calendar 4→1 入口折叠 ship**(第 4 刀,模板复用 #1) | 2-stage(audit + 落代码)单 commit ship;apple_calendar.py 4 旧 cap → 1 `apple_calendar` dispatcher + 4 `_handle_*` internal + **末尾 2 行 module-level alias**(backward-compat for calendar.py D1 router);tool_addendum.py 3 处引导微改;3 smoke 全过(ToolRegistry / 4 LLM query 双路径覆盖含 chunk-1 time.now→apple_calendar 链路 / dispatcher routing 4 档);**实测 -91 token (P1.media 9,697 → 9,606 / 27.5% 累计)**;Smoke 2 暴露 calendar router 反向 import 漏 audit → lesson #7 双 grep 模式 + lesson #8 dispatcher 实写 chars 比预估高 30-80% 记入 §2.7.4 | INVESTIGATION-6.md §3 |
| 2026-05-21 | **INV-6 §2 · 子轨 B P1.media 5→1 入口折叠 ship**(第 3 刀,dispatcher 模板) | 3-stage(audit/design + PM 拍板 5 决策 + 落代码)单 commit ship;media_control.py 5 旧 cap → 1 `media` dispatcher + 5 `_handle_*` internal;tool_addendum.py 6 处引导微改 + CapabilityPanel.tsx 注释 1 行;3 smoke 全过(ToolRegistry / 5 LLM query 全调 media / dispatcher routing 三档);**实测 -257 token (P3 9,954 → 9,697 / 26.8% 累计)**;§2.7.4 抽象 dispatcher 模板 6 要点给后续 apple_calendar/bilibili/netease 复用 | INVESTIGATION-6.md §2 |
| 2026-05-21 | **INV-4 封存 + INV-6 起用** | INV-4 共 1,120 行封存(子轨 B §1 枚举 + §2 三分 + §3 评估 + §3.2.1 P2 改动提案 + §3.7 P2 实施收口全覆盖);后续实施(P3 起 + P1 入口折叠)迁 INV-6 | INVESTIGATION-INDEX.md + INVESTIGATION-4.md §封存说明 |
| 2026-05-21 | **INV-4 §3 · 子轨 B · 三类候选评估 + v4.1 实施清单** | P1 入口折叠 4 group(bilibili 11→1 / netease 13→2 / media 5→1 / apple_calendar 4→1)估省 **~5,960 tokens** + dispatcher 设计草图 + tool_addendum 引导重写;P2 desc 精简 top 10 长 cap 估省 **~700 tokens**;P3 character.set_activity 与 `<state_update>` tag 100% 重叠,clean cut 退役 ~150 tokens;PM §2.5 估 8-10k 校正为 **~6.8k**(扣 union schema 开销);v4.1 实施清单 6 动作排序按风险×工程量×收益;P4 MCP lazy-load 挂 v4.1+ backlog | INVESTIGATION-4.md §3 |
| 2026-05-21 | **INV-4 §3.7 + P2 desc 精简 ship**(子轨 B 实施第 1 刀) | Stage 1 草稿(7234afc)+ Stage 2 落代码(72808ef)双 commit;10 cap description 精简 3,670 → 2,369 chars;**实测 tools_schema 13,250 → 10,336 tokens (-2,914 / 22% reduction)**;Mai 行为零 regression(\<thinking\>/\<state_update\>/\<motion\> 全套 inline tag 正常);估 vs 实测差异 **预估的 4 倍**(Qwen tokenizer 长 desc 真实密度 ~1.75 token/char 而非估算 0.4) | INVESTIGATION-4.md §3.2.1 / §3.7 |
| 2026-05-20 | **INV-4 §2 · 子轨 B · proactive/reactive 三分审计** | 58 个 LLM 可见 capability 全部分类:**1 proactive**(character.set_activity) / **5 hybrid**(全 calendar+time 类) / **52 reactive**;严格判定下"真 proactive"远低于 PM 预测 ≤5;notable 5 条(proactive 稀缺/save_memory 与 brief 例矛盾/screen.* 边缘/snooze_wake_call misnomer/hybrid 全 calendar+time);§3 优先级建议主战场 = bilibili+netease+media+apple_calendar 33 cap 入口折叠 | INVESTIGATION-4.md §2 |
| 2026-05-20 | **INV-5 §5 · 子轨 A · 路径 F 真实施 + 真机回归** | PM 选路径 F，4-phase 推进 8 commit ship（Phase 2 renderer tuple + Phase 3 inject_cache_marker + Phase 4 prefix 切 + probe schema 扩 + bug fix）；DB id=16 active 行 apply（dashscope/qwen3.5-plus）；main_chat 真机 cold/warm 实测 `cached_tokens=0→5,655` 完美命中，99.8% 覆盖率；ROI 与 §3.4 预测 ~27% 吻合；8 非主链 caller marker no-op 无 regression；migrate_provider_prefix.py idempotent + rollback ready | INVESTIGATION-5.md §5 |
| 2026-05-20 | **INV-5 §4 · 子轨 A · T5 实测 DeepSeek 自动 caching = 绿档** | preflight 改 DB lookup 后跑通：`prompt_cache_hit_tokens 2,816 / 2,921 = 96.4% 覆盖率`，**含 tools= 列表全自动 cache**（与 Qwen `dashscope/` T4 silently strip 完全相反）；ROI 外推主路径 ~75%（vs 路径 F Qwen system-only 27%）；新三选一 D/E/F，CC 倾向 F + D 列入 v4.1 A/B 评测候选；陪伴质量风险面 = Mai 中文 persona 切 LLM 需真机评测 | INVESTIGATION-5.md §4 |
| 2026-05-20 | **INV-5 §3 · 子轨 A · T4 实测 tools= 列表 cache_control** | T4 = ⚠️ 档加深版：`dashscope/` + tools[-1] 顶层 cache_control 被完全 silently strip（cached_tokens null / 无 cache_creation 字段），与 T1 `openai/` 响应字段模式同；ROI 从 brief 假设 ~79% 缩水到 **~27-30%**（仅 system 段可缓存）；CC 倾向**路径 1 仅缓存 system 段**，tools 大头交给 INV-4 子轨 B 正面攻；零产品代码改动 | INVESTIGATION-5.md §3 |
| 2026-05-20 | **INV-5 §2 · 子轨 A · cache_control pass-through 实测裁决** | 三测全跑通：T1 `openai/` + cache_control = silently strip（无命中）/ T2 `dashscope/` + cache_control = **完美命中 1214 cached_tokens** / T3 `openai/` + 无 marker = implicit cache 客户端不可见；方案 a 实施路径明确 = **切 `dashscope/` prefix + 注入 cache_control**；外推主路径理论省 ~79% prompt token；零产品代码改动；脚本落 scripts/cache_probe_T1-3.py | INVESTIGATION-5.md §2 |
| 2026-05-20 | **INV-5 · 子轨 A · prompt caching 装配结构勘查** | grep 绿地确认 + 6 问事实链：messages[0] 单 system 大 string、layer 渲染 join 大 string、tools_schema 走 tools= 参数、字节稳定性代码层 OK 但物理结构无 marker 落点；**brief 假设需校正**：Skyler `openai/qwen-...` prefix 走 OpenAI 路径 + DashScope endpoint，LiteLLM 是否 pass-through cache_control 官方未明示需实测；CC 倾向方案 a（content blocks + cache_control），前置 2 个实测点 | INVESTIGATION-5.md §1 |
| 2026-05-20 | **INV-4 · token 第三刀（工具治理）第一步：全 capability 枚举** | PM 切轨：放弃追 43-68k 幽灵，治理 tools_schema 13.25k 大头；本步纯只读枚举全 capability（含 @register_capability + MEMORY_TOOLS + builtin + MCP active），形成 proactive/reactive 二分前置表 | INVESTIGATION-4.md §1 |
| 2026-05-20 00:21 | **INV-3 · token 治理轮 第一刀（如实收口）** | 原假设代码层双排除；主路径 40+ 轮实测封顶 ~22.7k，43-68k 未复现；tools_schema 13.25k 实测确认；summary 全 0 但 extractor 活（矛盾）；真凶疑在未观测的主动/后台链，转第二刀（⑤ 待查清单 6 条，含 fold 机制认知分歧待代码裁决）；本轮续：A 修复落地生效 / ⑤.3 实证结案（fold 正常，summary 0→465）/ short_term 改 25 生效；待办：⑤.2 主动对话 + 工具懒加载（高危大头）；backlog 首字延迟 31→71s 恶化；§⑩ 第二刀勘查：三条目标链 LLM 调用全走 client.py 统一封装，proactive_engine 无独立调用点，43-68k 推断转 backlog；详 INV-3 §⑩ | INVESTIGATION-3.md |
| 2026-05-19 22:21 | **docs 整理轮（归档第一刀 + 真源对齐第二刀）** | 40 份 .md 治理；A 方案：DESIGN.md 冻结归档 / DESIGN_LITE 升真源；第一刀 19 份 git mv R100 零字节改；第二刀 5 真源 6 类改动（死链 / 退役同步 / HEAD 锚 / 新成果补录 / LITE 补位 / 新挂起项）；"未 commit" 硬校验通过 | INVESTIGATION-2.md |
| 2026-05-19 15:30 | **性能治法弹药** | persona Mai 实测 2,759 tokens（修正上一版误判，进 Top-3 最肥块）；ADDENDUM 97.8% 真增量难压；活动 timeline 有硬 cap 非膨胀；最大膨胀仍是工具 schema 11.1k（懒载理论可省 9-10k） | INVESTIGATION-2.md |
| 2026-05-19 15:09 | LLM prompt token 分块账单 | activity timeline 8h heavy 154 tokens 有硬 cap；首要膨胀 = tool schemas 11.1k + ADDENDUM 3.2k 固定 95% | INVESTIGATION-2.md |
| 2026-05-19 04:52 | 前端全面重核 + FRONTEND-OVERVIEW 校准 | §3.2 5 条争议项判错 4 + 1 半对；SystemStatus 仅前端面板不进 LLM；MemoryViewer / VoiceButton 实测死代码 | INVESTIGATION.md |
| 2026-05-19 04:24 | clipboard 溯源 + 前端勘查误判自查 | clipboard UI 真有（Settings ClipboardSection 160 行），原判错；agent 报告整段抄录是失误根因 | INVESTIGATION.md |
| 2026-05-19 03:51 | memory.confidence 字段溯源 | confidence 是 chunk 10 (2026-05-12 a692ac9) 早就有的字段，validator 入库前阈值过滤真活，c1d65ff 零触动 | INVESTIGATION.md |
| 2026-05-19 02:58 | todos 退役 + profile_summary 条件核删 | 删 12 文件（agents/memory + planner + scheduler/task 等）；delete_memory 活路径零触动；profile_summary 真死一并删 | INVESTIGATION.md |
| 2026-05-19 02:32 | todos 退役 recon | apple_calendar 是 macOS EventKit 内建非 MCP；AlarmScheduler 真活但 owner_type='alarm' 零写入源；prompts.py 幽灵工具 add_todo/personality 等可整删 | INVESTIGATION.md |
| 2026-05-19 02:21 | 前后端对齐 · profile 与 todos 核实 | `/api/profile` GET 只显 legacy `profile_summary`；真"用户画像"是 `profile_data`；todos LLM 写入路径已完全断（MemoryAgent dead） | INVESTIGATION.md |
| 2026-05-19 01:16 | 后端收尾 · 文档止血整合 | ROADMAP 1477 行历史外迁至 IMPLEMENTATION_LOG.md（byte-perfect sha1 校验）+ DESIGN_LITE §4 补 conversation_summary / mcp 三表 | INVESTIGATION.md |
| 2026-05-19 01:03 | 第二刀 · 删 12 孤儿 character_states | 12 孤儿 cid (300-700) 全 DELETE；character_states 21→9 行；零 sibling 表残留 | INVESTIGATION.md |
| 2026-05-19 00:49 | 第一刀 · switch_character 充分档 + 孤儿核实 | registry.py 删 switch_character 注册；prompts.py + tool_addendum.py 删引导文字；12 孤儿 character_states 调研结论"零活消费者可安全删" | INVESTIGATION.md |
| (2026-05-18 起前期) | 第 1-4 节 · switch_character 调用链 + characters DB 全貌 + 悬而未核发现 + 调研结论 | switch_character 仅 builtin tool，前端切角色走 WS frame；characters 表 9 行（cid=1 Mai 借壳，101 独立 ja 模式）；M-6 ChatHistoryDrawer 真清 + L-6 SHORT_TERM_MAX=30 + L-7 voice_samples 真注入 + L-8 cid=1 voice 已 Z.8 改 zh | INVESTIGATION.md (§1-§4) |

---

## 主题聚类（同主题跨刀次串读用）

### -3. TTS pipeline + 实时 input device API (INV-15)

- 2026-05-27 **INV-15 P2 三件套 ship**(commit 8537393)— VAD mic stream stale recovery + diagnostic UI · Option H 健康检查 + Option I onended recovery + Option G real-time max bar · Lesson #22 沉淀(实时 input 三件套缺一不可)
- 2026-05-27 **INV-15 P1 Option A ship**(commit 534a6ca)— `merge_short_sentences` 扩到 zh 路径 · 删 ja/en-only gate · Lessons #20/#21 沉淀
- 2026-05-27 **INV-15 audit** — audio chunk gap 真因 = TTS consumer FIFO HOL blocking on cosyvoice cloud 偶发 9s outlier · chunk 15 closed 假设破 · INV-13/14 explicit 无关;VAD 卡空闲推测 threshold 太高 / stream stale 缺 recovery

### -2. VAD / ASR 可达性 (INV-14)

- 2026-05-27 **INV-14 P2 revert · UI re-expose 冗余 撤销**(commit aed67cc)— PM 真机 verify VAD UI 一直在 Capabilities → ASR tab · 主设置加冗余 revert · **Lesson #19 修正**(audit 前先 visibility verify · "用户找不到" ≠ "入口缺失")
- 2026-05-27 **INV-14 VAD 恢复 ship**(commits c2d8924/3f24d6c · P2 后即 revert)— backend HF_HUB_OFFLINE 硬编码移除(P1 保留)· UI re-expose 试 ship 后撤
- 2026-05-27 **INV-14 VAD 监听消失 audit** — VAD 代码完整未删 · 真因主要是 backend ASR broken(HF_HUB_OFFLINE + cache miss → 29/29 preload 失败)· ROADMAP P2 backlog 已记但未 fix · `.env` parse bug 同期发现 · audit 误推主设置 UI 入口失踪后被 PM verify 推翻

### -1. Audit 方法论 + proactive/short_term 互动(INV-13)

- 2026-05-27 **INV-13 Option F+G ship**(commits 0b38cd6/659351e + C3 docs)— ja-aware 条件注入 + 软化字数约束 + main.py:484 deprecation comment;触发 Lesson #16/#17/#18 沉淀
- 2026-05-27 **INV-13 §12 · "聊着聊着被带坏" verify**(数据反驳累积 drift hypothesis)— conv=62 20-turn JA streak counter-example;真因 = v4_0_0_mai_revert_zh migration pre-hotfix bug(fd11d74 已闭合)
- 2026-05-26 **INV-13 §1-§11 · proactive → short_term ja-format 污染 audit**(PM challenge 后扩大)— ja directive 同源注入(proactive 不缺)· DB 14 turn 实证(proactive 100% ja > normal 90%)· Option D 误诊 drop · refined Fix F+G+H

### 0. TTS provider × model × voice paradigm (INV-9 / INV-11 / INV-12)

- 2026-05-26 **INV-11 Stage 1.5 followup**(INVESTIGATION-INDEX 主表 + `docs/adding-new-tts-model.md`)— plus label fix + section headers + tts_models.json json config + pydantic validation + GSV 2 mode schema 预留 + playbook + Lesson #13/#14/#15 沉淀;hotfix VoicePicker model 字段补足
- 2026-05-26 **INV-11 Stage 1.5 paradigm**(`backend/tts/registry.py` + `frontend/src/components/character/VoicePicker.tsx`)— 全角色 provider × model × voice 三级解耦;VoicePicker 经 modal → inline paradigm B 重构(Lesson #12)
- 2026-05-25 **INV-11 Stage 1 GSV 真接入**(`backend/tts/gsv.py`)— mai_v4 emotion-aware ja TTS · GSV server lazy weights init + 16 emotion bank LLM 路由 · Lesson #11 fallback 阶段化
- 2026-05-25 **INV-12 Stage 2 Fish backend**(`backend/routes/tts_api.py`)— user_override 配对约束 + 4 endpoints
- 2026-05-22 INV-9 §7 cost cap + cid=101 fish lock(Phase 2 closed)
- 2026-05-22 INV-9 中插 part 1/2/3 · Fish 参数 sweep + stochastic verdict(Lesson #6/#7/#8)
- 2026-05-22 INV-9 §5+§6 · per-provider [bracket] markers 双重隔离(Lesson #4/#5)
- 2026-05-22 INV-9 §2+§3+§4 · Fish provider ship(Lesson #2/#3)
- 2026-05-22 INV-9 §1 · sanitize A1 fix(Lesson #1)
- 2026-05-22 INV-8 §1 · Phase 1 audit closed

### 1. 性能 / Token 治理（持续主题）

- 2026-05-21 backlog 重定位（per 纪律「调查/audit/评测 → INV files;实施 → ROADMAP」）— **INV-3 §10.9 + 1 backlog**（extractor 5-min 频率 audit）;**INV-5 §5 + 2 backlog**（Qwen Plus vs Max / Qwen Plus vs DeepSeek 评测候选,新增 §5.6）;**INV-7 §2.8 + 1 backlog**（`_CAPABILITY_TAG_RE` 容忍单字 cap-name 兜底,P1.netease 子轨 B 收尾后挂起）
- 2026-05-21 **INV-7 §2 · P1.netease 13→2 双 dispatcher fold ship + 子轨 B closure**(INVESTIGATION-7.md §2 + § 收尾归档)— 模板复用 #3 收尾刀,4 文件改 + 双 dispatcher web/local 路径差异化 + 3 smoke 全过(14 query 含边界 case),**实测 -1,136 token / 44.9% 累计**(P1.bilibili 8,437 → 7,301);**子轨 B 整轮 5 commit closed,累计 -5,949 token / -44.9%**(P2 + P3 + P1.media + P1.apple_calendar + P1.bilibili + P1.netease);lesson #10 接受 trade-off 挂 backlog
- 2026-05-21 **INV-7 §1 · P1.bilibili 11→1 fold ship**(INVESTIGATION-7.md §1)— 模板复用 #2 最大头,3 文件改 + frontend retro-fix + 3 smoke 全过(11/11 LLM query 调 bilibili),**实测 -1,169 token / 36.3% 累计**(P1.apple_calendar 9,606 → 8,437);lesson #9 三 grep 模式记入
- 2026-05-21 **INV-6 封存(915 行) + INV-7 起用** — INV-6 子轨 B 实施前 3 刀完整收口,后续 P1.bilibili + P1.netease 迁 INV-7
- 2026-05-21 **INV-6 §3 · P1.apple_calendar 4→1 fold ship**(INVESTIGATION-6.md §3)— 模板复用 #1,2 文件改 + alias 兜底 + 3 smoke 全过(双路径覆盖含 chunk-1 chain),**实测 -91 token / 27.5% 累计**(P1.media 9,697 → 9,606);lesson #7 双 grep audit + #8 dispatcher schema chars 比预估高 30-80% 记入 §2.7.4
- 2026-05-21 **INV-6 §2 · P1.media 5→1 fold ship**(INVESTIGATION-6.md §2)— dispatcher 模板首刀,3 文件改 + 3 smoke 全过,**实测 -257 token / 26.8% 累计**(P3 9,954 → 9,697);§2.7.4 抽象 6 要点给后续 fold 复用
- 2026-05-21 **INV-4 封存(1,120 行) + INV-6 起用**(INVESTIGATION-INDEX 顶部封存说明 + 主表登记) — 子轨 B 后续实施(P3 起 + P1 入口折叠)迁 INV-6
- 2026-05-21 INV-4 §3.7 + P2 desc 精简 ship(INVESTIGATION-4.md §3.2.1 / §3.7)— Stage 1 + Stage 2 双 commit(7234afc + 72808ef);**实测 tools_schema -2,914 tokens / 22% reduction**,估 vs 实测 4 倍,Qwen tokenizer 长 desc 真实密度 ~1.75 token/char 修正方法论
- 2026-05-21 INV-4 §3 子轨 B · 三类候选评估 + v4.1 实施清单（INVESTIGATION-4.md §3）— P1 fold ~5,960 + P2 desc ~700 + P3 set_activity 退役 ~150 = **~6.8k tokens 收益**;6 动作按风险×工程量排好序;与子轨 A 5.6k 叠加主路径 prompt 砍 ~55%
- 2026-05-20 INV-4 §2 子轨 B · proactive/reactive 三分审计（INVESTIGATION-4.md §2）— 58 cap 分类 1 proactive / 5 hybrid / 52 reactive;严格判定下 proactive 远低 PM 预测 ≤5;§3 优先级建议 33 cap 入口折叠主战场
- 2026-05-20 INV-5 §5 子轨 A · 路径 F 真实施 + 真机回归（INVESTIGATION-5.md §5）— PM 选 F，4-phase 8 commit ship；main_chat WARM 5,655 cached_tokens 99.8% 覆盖率完美命中；8 非主链 caller no-op 无 regression；DB+config+probe+migrate script 全 ready
- 2026-05-20 INV-5 §4 子轨 A · T5 实测 DeepSeek 自动 caching = **绿档**（INVESTIGATION-5.md §4）— preflight 改 DB lookup 后跑通：96.4% 覆盖率含 tools= 全 cache；外推 ROI ~75%；新三选一 D/E/F，CC 倾向 F + D 列入 v4.1 A/B 评测
- 2026-05-20 INV-5 §3 子轨 A · T4 实测 tools= 列表 cache_control（INVESTIGATION-5.md §3）— tools 段 cache_control silently strip；ROI 从 ~79% 缩水到 ~27-30%；CC 倾向路径 1（仅缓存 system 段）+ INV-4 子轨 B 正面攻 tools_schema
- 2026-05-20 INV-5 §2 子轨 A · cache_control pass-through 实测（INVESTIGATION-5.md §2）— T2 `dashscope/` prefix 完美命中 1214 cached_tokens；T1 `openai/` prefix silently strip；T3 implicit cache 客户端不可见；方案 a 路径明确 = 切 `dashscope/` prefix + 注入 cache_control，理论省 ~79%
- 2026-05-20 INV-5 子轨 A · prompt caching 装配结构勘查（INVESTIGATION-5.md §1）— 6 问事实链 + brief 假设校正（`openai/qwen-...` prefix vs LiteLLM 原生 `dashscope/` prefix 路径分流）+ 三方案 proposal，CC 倾向方案 a 前置 2 实测点
- 2026-05-20 INV-4 第三刀 · 工具治理（INVESTIGATION-4.md §1）— PM 切轨砍 tools_schema 13.25k 大头；第一步全 capability 枚举（proactive/reactive 二分 + tag-conversion 候选 + 入口折叠候选评估前置）
- 2026-05-20 00:21 INV-3 第一刀 · 如实收口（INVESTIGATION-3.md）— 40+ 轮实测封顶 22.7k 未复现 43-68k；tools_schema 13.25k 实测；summary 全 0 ↔ extractor 活的矛盾；真凶疑在主动/后台链，转第二刀（⑤ 6 条，含 fold 机制认知分歧）；§⑩ 第二刀勘查 LLM 调用全走 client.py 统一封装，43-68k 转 backlog
- 2026-05-19 15:30 性能治法弹药（INVESTIGATION-2.md）— 修正 persona 误判 + 懒加载地形
- 2026-05-19 15:09 token 分块账单（INVESTIGATION-2.md）— activity timeline cap 验证

### 2. 前端勘查 + 失误教训

- 2026-05-19 04:52 前端全面重核（INVESTIGATION.md）— §3.2 5 条判错 4
- 2026-05-19 04:24 clipboard 溯源 + 误判自查（INVESTIGATION.md）— 失误根因

### 3. todos / profile_summary 退役链

- 2026-05-19 02:58 退役实施（INVESTIGATION.md）— 12 文件改动 commit c1d65ff
- 2026-05-19 02:32 退役 recon（INVESTIGATION.md）— 替代者 apple_calendar 确证
- 2026-05-19 02:21 前后端对齐核实（INVESTIGATION.md）— 写入路径已断

### 4. character_states 孤儿清理

- 2026-05-19 01:03 第二刀 DELETE 12 行（INVESTIGATION.md）
- 2026-05-19 00:49 第一刀 + 孤儿调研（INVESTIGATION.md）

### 5. switch_character + characters 命名

- 2026-05-19 00:49 第一刀 + commit 71b6e99（INVESTIGATION.md）
- 早期 INVESTIGATION.md §1-§4 调研

### 6. memory 字段 + schema

- 2026-05-19 03:51 confidence 溯源（INVESTIGATION.md）

### 7. 后端收尾 / 文档止血

- 2026-05-19 01:16 ROADMAP 外迁 + DESIGN_LITE 补表（INVESTIGATION.md）

### 8. 文档治理（新增主题）

- 2026-05-19 22:21 docs 整理轮 — 归档第一刀 + 真源对齐第二刀（INVESTIGATION-2.md）—— A 方案 / 4 政策决策 / 19 份归档 R100 零字节 / 5 真源 6 类改动 / "未 commit" 硬校验

---

## 备注

- 本索引仅记录正式刀次 / 调研段。零碎 bug fix 不入。
- 每次 INVESTIGATION-2.md 追加新刀次时，本表同步加一行。
- INVESTIGATION.md 已封存不再追加；新刀只入 INVESTIGATION-2.md。
- "一句话结论"力求承重不修饰，便于跨周回看快速定位。
