node_modules/
.venv/
venv/
__pycache__/
*.pyc
*.pyo
target/
frontend/src-tauri/target/
frontend/dist/
*.db
*.db-journal
*.db-shm
*.db-wal
*.db.backup-*
*.db.backup_*

# hotfix-8: config.yaml duplicate-block 备份（防未来 hotfix 期间一次性 snapshot 误 commit）
config.yaml.backup-before-*

# 2026-06-15 ④ MCP 拆文件 · 真实清单(8 + 7 entry · 含 PM 个人 path / 凭证 key 引用)
# 入库的是 mcp.config.example.yaml(零凭证 4 范式模板)
mcp.config.yaml

# 2026-06-19 · 一次性探针 / 临时调试脚本 · 不入 git
# 例:scratch/step0_vl_probe.py(图片输入 vision 能力探针 · 跑完留本地可复跑)
scratch/

.env
.DS_Store
*.log
*.mp4
.vscode/
.idea/

# 防止 dump 和敏感数据进 git
*_dump.txt
*_dump.json
*.dump
*.sql
.claude/

# CC 内部 chunk 报告（每个 chunk 生成一份 markdown，本地工作记录，不进 git）
.skyler-cc-reports/

# 测试脚本生成的音频样本（独立验证用，体积大、可重复生成，不进 git）
tools/output/

# ==============================
# Live2D 角色资产 IP 风险隔离 (v3-E2)
# ==============================
# 默认排除 frontend/public/live2d/ 下所有子目录（角色资产），仅白名单：
#   - core/   ← Cubism Core JS SDK runtime（不是角色资产，必须入库）
#   - hiyori/ ← Live2D 官方免费样品（已确认 license OK）
# 第三方 / 同人模型（八重神子 / 加藤惠 / 自购委托等）放进对应 slug 目录后
# 会自动 ignore，避免 IP 资产意外进 git 历史。增加新白名单角色（自制 / 已
# 完成 license 清理的）时在下面追加 `!frontend/public/live2d/<slug>/`。
frontend/public/live2d/*/
!frontend/public/live2d/core/
!frontend/public/live2d/hiyori/

# ==============================
# v3.5 chunk 5a: per-character backgrounds (IP 隔离)
# ==============================
# 默认排除所有用户素材（image / video）；只白名单 README + .gitkeep 让目录
# 本身入库，避免 frontend/public/backgrounds/ 完全缺失。
frontend/public/backgrounds/*
!frontend/public/backgrounds/.gitkeep
!frontend/public/backgrounds/README.md

# ==============================
# v3.5 chunk 5b: 启动入场 splash video（用户离线生成）
# ==============================
frontend/public/splash/*
!frontend/public/splash/.gitkeep
!frontend/public/splash/README.md
api_key.txt
scripts/fish_probe_outputs/

# INV-17 v3 (2026-05-28): silero VAD + onnxruntime-web runtime assets
# 由 frontend/scripts/copy-silero-assets.mjs 在 npm run predev / prebuild 时从
# node_modules 复制 · 体积 ~38MB · 不入 git。新 clone repo 跑 npm install 后
# `npm run copy-silero`(或自动 predev / prebuild trigger)生成。
frontend/public/silero/

# v4.0 voice greeting · 用户上传 audio 存盘(不进 git,per Live2D / splash-art
# 同款 IP 隔离 + 体积考虑;迁机器靠 backup script)
backend/static/voice_lines/

# INV-12 Fish TTS 配置管理 · 用户上传 reference audio 存盘(同款 IP 隔离)
backend/static/fish_references/

momoos.db*
logs/
# /tts/ 仅 ignore 顶层(emotion bank wav + 用户 ref audio · IP 隔离);
# backend/tts/*.py 源码 (含 INV-11 gsv.py / registry.py) 必须入库。
# 历史 typo `tts/` (无前导斜杠 / 匹配任意深度) 误吞 backend/tts/gsv.py + registry.py。
/tts/
test1111.py
voice_clone_local.py
frontend/src/assets/
