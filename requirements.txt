fastapi
uvicorn[standard]
websockets
sqlalchemy[asyncio]
aiosqlite
litellm
httpx
faster-whisper
edge-tts
dashscope>=1.25.2
python-dotenv
pydantic
pydantic-settings
sentence-transformers
numpy
pyyaml
apscheduler>=3.10
# v3-G chunk 1 — Google Calendar OAuth + API client + retry
google-auth>=2.30
google-auth-oauthlib>=1.0
google-api-python-client>=2.100
tenacity>=8.0
# v3-G chunk 1.5 — MCP 双向集成（server + client）
mcp>=1.0
# v3-G chunk 1.6 — Apple Calendar via macOS EventKit（仅 macOS 实际加载）
pyobjc-framework-EventKit>=10.0; sys_platform == "darwin"
# v3-H chunk 1 — 网易云 web API 加密（weapi: AES-128-CBC + RSA-1024）
pycryptodome>=3.20
# v3-G chunk 3a — 剪贴板助手。AppKit (NSPasteboard) 在 macOS 上是 EventKit 的
# 同 pyobjc 包 transitive 依赖，无需额外声明；非 macOS 走 pyperclip fallback。
pyperclip>=1.8
# v3.5 chunk 7 — docx capability（python-docx 处理 .docx 文档）
python-docx>=1.0
# 2026-06-19 · 文件输入 MVP — PDF 抽文本(pypdf 4.x · 纯 python · 无 native 依赖)
pypdf>=4.0
# v3.5 chunk 6a — B 站接入（Nemo2011/bilibili-api 社区 fork，活跃维护）
bilibili-api-python>=17.4
# v3.5 chunk 8a — 公开 URL 正文提取（readability-lxml + 内置 lxml-html-clean）
# ActivityWatcher 后台 fetch 浏览器当前 tab 的公开页面时用，让 LLM 看到正文。
readability-lxml>=0.8
# v4 persona engineering segment 1 — 5-layer prompt templates (Jinja2)
jinja2>=3.0
mutagen>=1.47.0
# 2026-05-28: faster-whisper 中文输出繁体 · OpenCC t2s 转简体 (asr/whisper.py)
opencc>=1.1
# 2026-06-04 · Round 5 step2 · OS 标准 appData 路径(等价 Tauri appDataDir(),
# 用 Tauri identifier "com.skyler.momoos" 作为 appname 对齐路径)。用户上传
# 的 background 资产落到 ~/Library/Application Support/com.skyler.momoos/backgrounds/。
platformdirs>=4.0
