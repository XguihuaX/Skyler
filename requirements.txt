fastapi
uvicorn[standard]
websockets
sqlalchemy[asyncio]
aiosqlite
litellm
httpx
faster-whisper
edge-tts
dashscope>=1.25.2
python-dotenv
pydantic
pydantic-settings
sentence-transformers
numpy
pyyaml
apscheduler>=3.10
# v3-G chunk 1 — Google Calendar OAuth + API client + retry
google-auth>=2.30
google-auth-oauthlib>=1.0
google-api-python-client>=2.100
tenacity>=8.0
# v3-G chunk 1.5 — MCP 双向集成（server + client）
mcp>=1.0
# v3-G chunk 1.6 — Apple Calendar via macOS EventKit（仅 macOS 实际加载）
pyobjc-framework-EventKit>=10.0; sys_platform == "darwin"
# v3-H chunk 1 — 网易云 web API 加密（weapi: AES-128-CBC + RSA-1024）
pycryptodome>=3.20
# v3-G chunk 3a — 剪贴板助手。AppKit (NSPasteboard) 在 macOS 上是 EventKit 的
# 同 pyobjc 包 transitive 依赖，无需额外声明；非 macOS 走 pyperclip fallback。
pyperclip>=1.8
# v3.5 chunk 7 — docx capability（python-docx 处理 .docx 文档）
python-docx>=1.0
